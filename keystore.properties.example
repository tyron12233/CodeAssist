# Copy to keystore.properties (gitignored) and fill in. Read by ide-android/build.gradle.kts.
# storeFile is resolved relative to the repo root.
storeFile=keystore/codeassist-upload.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=upload
keyPassword=YOUR_KEY_PASSWORD

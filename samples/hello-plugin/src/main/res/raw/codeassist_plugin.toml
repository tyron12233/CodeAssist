[plugin]
id = "com.example.hello"
name = "Hello Plugin"
version = "1.0.0"
apiVersion = 1
description = "Sample plugin shipped as its own app: a settings page, a palette command, and an editor action."
entryPoints = ["com.example.hello.HelloPlugin"]
capabilities = ["ui.settingsPage", "ui.action", "ui.editorAction"]
minHostVersion = "3.12.0"
